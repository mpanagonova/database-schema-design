version: "3.8"

services:
  db:
    image: postgres:16
    container_name: db_schema_design
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
      POSTGRES_DB: testdb
    ports:
      - "5432:5432"